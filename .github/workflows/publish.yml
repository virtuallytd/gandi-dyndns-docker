name: publish
on: [push]
jobs:
publish-gandi-dyndns-docker-image:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v2
- name: Login to GitHub Container Registry
uses: docker/login-action@v1
with:
registry: ghcr.io
username: ${{ github.actor }}
password: ${{ secrets.GITHUB_TOKEN }}
- name: Build the gandi-dyndns-docker Docker image
run: |
       docker build . --tag ghcr.io/virtuallytd/gandi-dyndns-docker:latest
       docker run ghcr.io/virtuallytd/gandi-dyndns-docker:latest
       docker push ghcr.io/virtuallytd/gandi-dyndns-docker:latest